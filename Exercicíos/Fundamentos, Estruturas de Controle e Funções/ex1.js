function calc(x, y) {
    soma = x + y
    sub = x - y
    mult = x * y
    div = x / y

    console.log(`Soma: ${soma}\nSubtração: ${sub}\nMultiplicação: ${mult}\nDivisão: ${div}`)
}

calc(10,2)